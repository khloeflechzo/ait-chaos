import React from 'react';

import s from './style.module.scss';

const LandingPage = (): React.ReactElement => {
  return <div className={`container ${s.main_wrapper}`}>hahaha</div>;
};

export default LandingPage;
